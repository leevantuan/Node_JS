<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta http-equiv="X-UA-Compatible" content="IE=edge" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Edit user's information</title>
        <!-- Latest compiled and minified CSS -->
        <link
            href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css"
            rel="stylesheet"
        />

        <!-- Latest compiled JavaScript -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/js/bootstrap.bundle.min.js"></script>
    </head>
    <body>
        <div class="container">
            <div class="row col-md-6">
                <form action="/put-crud" method="POST">
                    <h1 class="mt-3 text-danger">Update user's information</h1>
                    <div class="form-row d-flex justify-content-between">
                        <div class="form-group col-md-12 mt-3">
                            <label for="inputEmail">Email</label>
                            <input
                                type="email"
                                class="form-control"
                                placeholder="Email"
                                name="email"
                                id="inputEmail"
                                value="<%= user.email %>"
                            />
                        </div>
                    </div>
                    <div class="form-row d-flex justify-content-between">
                        <div class="form-group col-md-5 mt-3">
                            <label for="inputFirstName">First Name</label>
                            <input
                                type="text"
                                class="form-control"
                                placeholder="First Name"
                                name="firstName"
                                id="inputFirstName"
                                value="<%= user.firstName %>"
                            />
                        </div>
                        <div class="form-group col-md-6 mt-3">
                            <label for="inputLastName">Last Name</label>
                            <input
                                type="text"
                                class="form-control"
                                placeholder="Last Name"
                                name="lastName"
                                id="inputLastName"
                                value="<%= user.lastName %>"
                            />
                        </div>
                    </div>
                    <div class="form-group mt-3">
                        <label for="inputAddress">Address</label>
                        <input
                            type="text"
                            class="form-control"
                            name="address"
                            placeholder="1234 Main St"
                            id="inputAddress"
                            value="<%= user.address %>"
                        />
                    </div>
                    <div class="form-row d-flex justify-content-between">
                        <div class="form-group col-md-7 mt-3">
                            <label for="inputphonenumber">Phone Numbers</label>
                            <input
                                type="text"
                                class="form-control"
                                placeholder="84+ "
                                name="phoneNumber"
                                id="inputphonenumber"
                                value="<%= user.phoneNumber %>"
                            />
                        </div>
                        <div class="form-group col-md-4 mt-3">
                            <label for="gender">Sex</label>
                            <select
                                name="gender"
                                class="form-control"
                                id="gender"
                                value="<%= user.gender %>"
                            >
                                <option value="1">Male</option>
                                <option value="0">Female</option>
                            </select>
                        </div>
                    </div>
                    <input name="id" type="text" value="<%= user.id %>" hidden />
                    <button type="submit" class="btn btn-primary mt-3 ps-3 pe-3">Update</button>
                </form>
            </div>
        </div>
    </body>
</html>
